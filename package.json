{
  "name": "moteur-de-jeu-cartev1",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w engine && npm run build -w web && npm run build -w server",
    "start": "npm run start -w server",
    "test": "npm run test -w engine && npm run test -w server",
    "dev": "concurrently -k -n server,web -c blue,green \"npm run dev -w server\" \"npm run dev -w web\"",
    "new-card": "npm run new-card -w engine --"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.6.3"
  }
}
